import java.util.ArrayList;

/**
 * Created by msimpson on 8/6/15.
 */
public class ForestFactory {

    private ArrayList<CarrierSet> forest = new ArrayList<CarrierSet>();
    private ArrayList<WeightedEdge> edgeList = new ArrayList<WeightedEdge>();

    public void construct(ArrayList<String[]> data) {
        edgeToAdd = new WeightedEdge(tokens[0], tokens[1], Integer.parseInt(tokens[2]));
        edgeList.add(edgeToAdd);

        for(int i = 0; i < l)

        compare = forest.get(forest.size()-1).getLabel();

        if(!tokens[0].equals(compare)) {
                forestToAdd = new CarrierSet(tokens[0]);
                forest.add(forestToAdd);
                }
            }
        }
}
